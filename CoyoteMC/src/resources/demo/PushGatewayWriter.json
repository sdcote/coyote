{
	"Class" : "coyote.dx.Job",
	"Description": [
		"This is a simple posting of a metric to a push gateway.",
		"The PushGatewayWriter uses a basic authenticator to get past the revers proxy into the locally hosted docker",
		"containers. Since the containers are operating on their own subnet within the Docker host, the reverse proxy ",
		"is required for outside outsize access into the Docker subnet. This provides a layer of security over the ",
		"normally open push gateway service and the other monitoring components."
	],
	"Job" : {
		"Name" : "StaticReader",
		
		"Reader" : {
			"class" : "StaticReader",
			"fields" : {
				"name": "cpu_percentage",
				"value": 42,
				"instance": "P2026071"
			}    
		},

		"Writer" : {
			"class" : "PushGatewayWriter",
			"target" : "http://localhost:9091",
			"authenticator": {
				"class": "BasicAuthentication",
				"username": "admin",
				"password": "admin",
				"preemptive": true
			}
		}
	},


	"Logging": {
		"StandardError": {"categories": "error, fatal, warn" },
		"StandardOutput": { "categories": "info, notice" }
	}

}
